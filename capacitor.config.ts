import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.raciocine.starter',
  appName: 'RACIOCINE',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
